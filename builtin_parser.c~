#include "holberton.h"
/**
 *builtin_parser - parses builtin programs
 *@tokens:split tokens from stdin
 *Return:0 - success,1 otherwise
 */
int builtin_parser(char **tokens)
{
	if (strncmp(*tokens, "cd", 2) == 0)
	{
		return(_cd(tokens));
	}
	if (strcmp(*tokens, "env") == 0)
	{
