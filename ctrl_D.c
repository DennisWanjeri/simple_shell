#include "holberton.h"

/**
 *ctrl_D -exit program if Ctrl-D is pressed
 *@i: character read
 *@command: user's typed in command
 *@env: environmental variable linked list
 *Return: nothing
 */
/*
void ctrl_D(int i, char *command, list_t *env)
{
  if (i == 0)
    {
      free(command);
      free(env);
      if (isatty(STDIO_FILENO))
	write(STDOUT_FILENO, "\n", 1);
      exit(0);
    }
}
*/
